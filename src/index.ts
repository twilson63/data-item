import base64url from "base64url";
import { DataItemCreateOptions } from './types'
import { DataItem } from './DataItem'

export function createData(
  data: string | Uint8Array,
  opts?: DataItemCreateOptions
): DataItem {

}